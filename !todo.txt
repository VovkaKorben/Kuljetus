1. добавить в city_changed request_transl(0)
2. формируем запрос к базе, если в sendapp всё ОК
3. # стираем все поля из localstorage, если в sendapp всё ОК

fields.json
все visible поля читаются из формы от юзера
not-visible читаются из функций (напр.  "retrieve": "get_timestamp" )
в функцию передается request

записали в базу заказ
прочитали из базы айди заказа
составили письмо
==============================================================================
в каждом инпуте висит 
data-init с json параметрами инициализации
<input type="text">

например
{
    'handler':''
    'param':''
}
JS после загрузки документа читает эти данные
оборачивает при необходимости элементами (dropdown etc)              
==============================================================================
любой элемент с data-lang="identifier" заменяется на
innerText (для тегов)
value (для input)
==============================================================================

data-type="dropdown"    data-type="simple"    data-type="date"  data-type="memo"

    